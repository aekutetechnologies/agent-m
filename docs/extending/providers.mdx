---
title: Providers
---

# Providers (OpenAI-compatible)

agent-m is model-agnostic over **OpenAI-compatible** endpoints — any service
with a `/chat/completions` API: OpenAI, DeepSeek, Groq, OpenRouter, Ollama,
LM Studio, Together, and more. The built-in `deepseek` remains the zero-config
default.

## Configure via settings.json

Add a `providers` array to `~/.agent-m/agent/settings.json`:

```json
{ "providers": [
  { "id": "openai", "name": "OpenAI", "baseUrl": "https://api.openai.com/v1",
    "model": "gpt-4o-mini", "apiKeyEnv": "OPENAI_API_KEY" },
  { "id": "local", "name": "Ollama", "baseUrl": "http://localhost:11434/v1",
    "model": "llama3.2", "contextWindow": 131072 }
]}
```

| Field | Meaning | Default |
|-------|---------|---------|
| `id` | `[a-z0-9-_]`; used by `--provider` and the key env `<ID>_API_KEY` | — |
| `name` | display name in `/model` | `id` |
| `type` | `anthropic` → native [Anthropic provider](/extending/anthropic); omitted/`openai` → OpenAI-compatible | OpenAI-compatible |
| `baseUrl` | no trailing `/chat/completions` | — |
| `model` | model id sent on the wire | — |
| `reasoning` | thinking deltas | `false` |
| `supportsImages` | vision parts | `false` |
| `contextWindow` | planning budget (gauge + compaction) | `128000` |
| `pricing` | `inMiss` / `inHit` / `out`, USD per 1M | `0` |
| `apiKeyEnv` | display-only hint naming the env var in errors; the key itself is read from `auth.json` / `settings.json` (see below) | `<ID>_API_KEY` |
| `models` | extra model ids offered in `/model` alongside `model` | well-known suggestions for `openai`/`anthropic`/`groq`/`openrouter` |

Reasoning models (`reasoning: true`) on OpenAI-compatible endpoints get a
`/variant` reasoning-effort selector (`Default`/`low`/`high`/`max`, or
`--variant` at startup), mapped to the `reasoning_effort` parameter; `max`
maps to `high`. DeepSeek models don't (their API has no effort parameter).
When `--provider <id>` is given without `--model`, the provider's `model` is
used — not a hardcoded fallback.

Keys are **never stored in the chat logs**. They are resolved from
`~/.agent-m/agent/auth.json` first, then `~/.agent-m/agent/settings.json`
(both accept the nested `{"providers": {"<id>": {"apiKey": "..."}}}` or
flat `{"<id>": "..."}` shapes, plus tolerant `KEY=value` lines). Putting the
key in `settings.json` works but logs a warning — that file is
world-readable (0644). Provider-key **environment variables are not read**
(`apiKeyEnv` only names the expected var in the missing-key error message).

Anthropic (native Messages API) is configured the same way with
`"type": "anthropic"` — see [Anthropic](/extending/anthropic). The
`/provider` wizard writes OpenAI-compatible entries; edit `settings.json`
directly for an Anthropic provider.

## `/provider`

`/provider` prints the number of models the active provider exposes; use
`/model <id>` to switch, `/tasks` to route roles (build/plan/compact/
subagent/refine) to providers, and `--provider <id>` at startup to select one.

- **1 add** — guided prompts: id, display name, base URL, model, key env var,
  context window. Saves to `settings.json` and live-switches the running agent.
- **2 edit** — pick a provider by number, update any field (pre-filled).
- **3 remove** — confirm, removes the entry.
- **4 switch** — pick a provider (or `0` for the built-in deepseek) and switch
  without restarting.
- `/provider <id>` switches directly.

The status bar shows the wizard prompt and the ⚡ marks the active provider.

## CLI

- `--provider <id>` — select a configured provider (or `deepseek`)
- `--list-models` — every configured provider's model, plus the built-in
  DeepSeek pair

Unknown provider ids fail with the list of configured ids rather than a
generic error.
