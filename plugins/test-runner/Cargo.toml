[package]
name = "agent-m-plugin-test-runner"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "Test-runner plugin for agent-m: detect and run the project's test command"

[lib]
crate-type = ["cdylib"]

[dependencies]
agent-m-plugin-sdk = { path = "../../crates/plugin-sdk" }
serde_json = { workspace = true }

[dev-dependencies]
tempfile = { workspace = true }
